<nav class="navbar navbar-expand-lg navbar-light bg-light">

    <!-- Brand -->
    <a class="navbar-brand" href="{{ path('index') }}">
      <img src="{{ asset('images/head64x64.png') }}"  width="32" height="32" alt="{{ brandname }}"> {{ brandname }}</a>

    <!-- Toggle Icon -->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Main Menu -->
    <div class="collapse navbar-collapse" id="navbarNav">

        <!-- left alligned -->
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <!-- home -->
            <li class="nav-item {% if app.request.attributes.get('_route') == 'index' %} active{% endif %}">
              <a class="nav-link" href="{{ path('index') }}">Home <span class="sr-only">(current)</span></a>
            </li>
            <!-- whatelse -->
            <li class="nav-item {% if app.request.attributes.get('_route') == 'whatelse' %} active{% endif %}">
              <a class="nav-link" href="{{ path('whatelse') }}">What else? <span class="sr-only">(current)</span></a>
            </li>
            <!-- user -->
            {% if is_granted('ROLE_USER_USER_VIEW') %}
                <li class="nav-item {% if app.request.attributes.get('_route') == 'stewie_user_list' %} active{% endif %}">
                  <a class="nav-link" href="{{ path('stewie_user_list') }}">User <span class="sr-only">(current)</span></a>
                </li>
            {% endif %}
        </ul>

        <!-- right alligned -->
        <span class="justify-content-end">
            <!-- usermenu -->
            {#% embed "@StewieUser/default/usermenu.html.twig" %#}{#% endembed %#}
        </span>
    </div>

</nav>
